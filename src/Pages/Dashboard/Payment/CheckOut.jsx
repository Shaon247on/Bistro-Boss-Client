
const CheckOut = () => {
    const handleSubmit = async (e)=>{
        e.preventDefault()

    }
    return (
        <form onSubmit={handleSubmit}>
            
        </form>
    );
};

export default CheckOut;